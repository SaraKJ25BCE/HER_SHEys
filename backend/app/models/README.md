Pydantic models / DB schemas go here as the real implementation grows
(e.g. `SkillProfile`, `SandboxDay`, `Booking`). Kept empty for now — the
request/response shapes are documented in `../../docs/API_CONTRACT.md`.
